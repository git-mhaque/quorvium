# Quorvium Specification (Deprecated)

The canonical product specification now lives in [`../spec.md`](../spec.md). This file is retained for historical reference; do not update it going forward. Instead, edit `spec.md` and keep cross-functional documentation in the `docs/` directory as needed.
